port = 8080
tailscale_socket = "/var/run/tailscale/tailscaled.sock"
enable_fuse=true

[protocol]
auto_protocols=true
auto_protocol_style="compatibility"
auto_generate_delay=60
protocols_enabled=["SFTP", "WEBDAV"]
